# Copy to .env and adjust paths/ports as needed
PORT=5173
# Where MDsrv is reachable from the browser. If running locally, use the same machine:
MDSRV_URL=http://127.0.0.1:8080
# Directory that MDsrv is serving (should include topology.pdb + traj.dcd)
DATA_DIR=./data